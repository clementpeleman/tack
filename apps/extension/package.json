{
  "name": "@tack/extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && vite build -c vite.config.content.ts && vite build -c vite.config.popup.ts && cp manifest.json dist/manifest.json && cp src/popup/index.html dist/popup.html",
    "dev": "vite build -c vite.config.content.ts --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tack/widget": "workspace:*",
    "preact": "^10"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.8",
    "vite": "^6"
  }
}
